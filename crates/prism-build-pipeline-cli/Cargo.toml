[package]
name = "prism-build-pipeline-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "CLI for the Prisma build pipeline (bake GI, cook assets, pack)"

[[bin]]
name = "prism-build-pipeline"
path = "src/main.rs"

[dependencies]
prism-build-pipeline = { path = "../prism-build-pipeline" }
anyhow = { workspace = true }
log = { workspace = true }
env_logger = { workspace = true }
clap = { workspace = true }
